export class Maths {
static PI = 3.14

constructor (numbers) {
    this.numbers = numbers
   
    
}
static max (a, b) {
    return Math.max (a,b)

}
static round (number) {
    return Math.round(number)
}

}
